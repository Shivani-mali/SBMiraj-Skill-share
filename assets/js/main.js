// Small UI niceties for Skill Share
document.addEventListener('DOMContentLoaded', function(){
    // Trim long bio text on load for elements with .text-truncate
    // No-op if Bootstrap handles it via CSS, but keep for future features.
    window.addEventListener('resize', function(){
        // placeholder for responsive behavior
    });
});
